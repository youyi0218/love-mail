# 设置默认行为，以防人们没有设置core.autocrlf
* text=auto

# 明确声明应该被规范化的文本文件
*.js text
*.css text
*.html text
*.json text
*.config text
*.log text

# 声明二进制文件，这些文件不应该被修改
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
*.pdf binary 